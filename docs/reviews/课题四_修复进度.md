# 课题四深度修复进度

更新时间：2026-09-09（北京时间）。状态：本轮针对性复核和补修已完成，全部原始架构问题尚未闭合。

## 最新交接（优先于下方历史记录）

用户最新要求：已自行修复评估报告，请针对性核验完成度，必要时亲自补修。已完成逐项复核并输出《课题四_修复完成度复核_2026-09-09.md》，其中列出十项状态、实际补修、测试证据与尚未解决的架构问题。续接必须先读该报告和最新用户消息，不得按旧文件列表覆盖用户的新修改。

本轮额外修复：低概率负证据因 floor 反升；即时跨日反馈不同步；反馈后重答刷分/伪独立证据；停用题提交；历史查询失败奖励首次；校准冻结折扣/排序/空历史/验证输出；相同时刻验证切分；零权重续期；知识库临时读取失败误删画像；截断事件历史仍合并；并发阅读去重；题目实际证据范围、重复选项/题干。

已通过真实 learning、learning-bench、repository、database 全包测试和 Learning handler 定向测试；三项并发路径 -race 通过；前端相关 37 项通过。扩大到整个 handler 时 TestRunStatelessSandboxCheckLocal 失败，未伪装成全仓通过。日志：.local-data/q4-audit-20260908/recheck-*.txt。

仍未完成：数据库事务/删除代际与多实例写入保护、题目内容版本、来源分离、晚到事件和版本化完整重放、推荐 replay 协议及真实有效性验证。完整清单以最新复核报告为准。历史权重未改写，旧重复作答累积的分数不会自动全部撤销。

当前额度已刷新（本轮开始查询五小时仅用 1%），本轮没有因额度不足停工。原先 weknora 自动任务的本地配置文件在 9 月 9 日检查时已不存在；不能继续声称它仍有效或已迁移到云端。云端迁移尚未执行，没有上传未提交改动或创建云端续接任务。

---

以下为 9 月 8 日的历史交接，非当前完成度：

## 用户授权与续接

用户授权依据两份课题报告和代码审查结果进行对应深度修复。主目录 F:\study\WeKnora，已有大量用户未提交修改，必须保留。不要提交、推送、部署或发送邮件。完整评估见同目录《课题四_代码与算法评估_2026-09-08.md》。两份源报告在 F:\大厂计划！\Weknora。

已创建当前任务续接自动任务，ID `weknora`，目标任务 `01a08165-0d43-7671-8e87-7b8cc3bfb4ec`，计划北京时间 2026-09-09 03:30 单次执行。五小时额度工具最后返回重置时间 2026-09-09 03:23:23、已用 83%，周额度已用 13%。续接先读取最新消息与本文件并重新查询额度。完成所有工作后停止自动任务；若仍不足，使用应用自动任务工具调整到下次刷新后。电脑及应用需要运行。

## 已确认的环境和基线

- HEAD：212f4826fca380f281a75323418868aaf115f2ae。课题四起始提交 63147224，基线父提交 412dcc41（VERSION 0.7.2）。
- Windows Go 测试此前出现 0xc0000139，已定位为 PATH 中的 MinGW 动态库冲突。每次测试前将 `(Split-Path (Get-Command gcc).Source)` 放在当前进程 PATH 最前面即可，无须更改系统配置。
- 真实包基线通过：`go test ./internal/application/service/learning ./cmd/learning-bench ./internal/application/repository -count=1`。记录 `.local-data/q4-audit-20260908/baseline-tests.txt`。
- 前端可使用 `node frontend/node_modules/tsx/dist/cli.mjs --test <测试路径>`。
- 早先审查的隔离复现和文档文本在 `.local-data/q4-audit-20260908/`，该目录被忽略。现在已有真实包测试，不应只依赖隔离替身。

## 本轮已修改，待回归

1. `frontend/src/views/knowledge/wiki/readTracker.ts`：隐藏时暂停并保留当前页面，返回时恢复；后台打开页面不计阅读时间；避免重复监听。待新增状态机回归测试。
2. `internal/application/service/learning/collect.go`：精准 chunk 引用优先，不再用同一文档回退命中其他概念；只有文档级引用才回退。待新增回归测试。

## 后续必须处理的审查项

按问题根因和改动风险分阶段实施，每阶段运行真实包测试并更新此文件：

- 主题映射缺少停采检查，可在删除后重建画像；需前后检查及研究跨请求/数据库事务的删除栅栏，不能把简单检查说成已解决并发删除。
- 前置边推断实际未按 40 对分批，且同批新接受边不进入环检测图；摘要截断函数无效。
- 已答对题只记录首次正确时间，导致全部题目首日答对后无法再取得间隔验证；重复作答最低 25% 权重可刷分；反馈后的即时重试不应成为独立能力证据。
- 衰减分数在错误答案到来时因时间戳重置反而上涨；FoldAll 对顺序敏感；晚到事件与状态重建必须使用确定的时间顺序，必要时增加状态算法版本。
- learning-bench 同时间答案泄漏、tenant/KB 混合、复演权重与生产不一致、缺少时间留出集；不能继续把原 35.94% 改善作为有效结论。
- 试题内容版本失效、引用必须来自实际给模型的证据、重复选项/题目过滤。
- 回放输入遗漏生产状态、未来页面/题目泄漏与推荐候选不公平；必须明确评估可证明的边界。
- Agent 自动读页面与用户主动阅读分离；同节点再次提问不应直接等价于不会。
- 写入尝试/事件/掌握度事务一致性、删除与在途任务隔离、reconcile 超限和事件无状态节点发现、合并迁移等架构问题。

不要未经数据验证宣称 FSRS/BKT 等必然显著胜出。实现修复后需给出通过的测试、仍未完成项及算法方案取舍。
